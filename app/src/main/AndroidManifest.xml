<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wearable.whatsfordinner">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MealsScreen" />
        <activity android:name=".RecipesActivity" />
        <activity android:name=".NewDishActivity" />
        <activity android:name=".RecipesScreen" />
        <activity android:name=".GroceriesScreen" />
        <activity android:name=".NewDishScreen" />
        <activity
            android:name=".NutritionGoalScreen"
            android:label="@string/title_activity_nutrition_goal_screen"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".EditedDishScreen" />
    </application>

</manifest>